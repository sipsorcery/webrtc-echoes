apiVersion: v1
kind: Service
metadata:
    name: "sipsorceryaksb2s-595b"
    labels:
        app: "sipsorceryaksb2s-595b"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "sipsorceryaksb2s-595b"